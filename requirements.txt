# Web framework
fastapi
uvicorn[standard]
sse-starlette        # SSE helper (optional but convenient for /stream)

# LangChain 1.0
langchain>=1.0.0
langchain-core>=1.0.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0

# RAG — vector store + embeddings
faiss-cpu
sentence-transformers

# Data validation
pydantic>=2.0.0

# UI
streamlit
requests

# Utilities
python-dotenv